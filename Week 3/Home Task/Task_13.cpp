#include<iostream>
using namespace std;

int main()
{
    int num, sum = 0;

    cout << "Enter 5 Integers:\n";

    cin >> num;
    sum = sum + num;

    cin >> num;
    sum = sum + num;

    cin >> num;
    sum = sum + num;

    cin >> num;
    sum = sum + num;

    cin >> num;
    sum = sum + num;

    cout << "Sum = " << sum;
}